<%= form_with(model: conversation, local: true) do |form| %>
  <% if conversation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conversation.errors.count, "error") %> prohibited this conversation from being saved:</h2>

      <ul>
      <% conversation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :datetime_conversation %>
    <%= form.datetime_select :datetime_conversation %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :datetime_closing_conversation %>
    <%= form.text_field :datetime_closing_conversation %>
  </div>

  <div class="field">
    <%= form.label :conversation_type_id %>
    <%= form.text_field :conversation_type_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
